#include <iostream>

int main() {
  int x =  4; // declare variable
  x = 5; // assignment, assign another number to x

  // if (condition) {
  // code
  // }

  int i = 11, k; // the value of i is 11 and k was also declared
  int *j;
  j = &i; // j is a pointer and has the address of i in memory
  k = *j; // 
}
